# ChibiOS Submodule Placeholder

This directory is reserved for the ChibiOS source tree.

Bootstrap note:

- In a fresh clone, initialize with git submodule workflow when repository metadata is available.
- Current scaffold compiles a `HAL_ChibiOS` abstraction without bundling third-party RTOS sources.
