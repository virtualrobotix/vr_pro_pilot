# ESP-IDF Submodule Placeholder

This directory is reserved for the Espressif ESP-IDF SDK tree.

Bootstrap note:

- In a fresh clone, initialize with git submodule workflow when repository metadata is available.
- Current scaffold compiles a `HAL_ESP32` abstraction without bundling third-party SDK sources.
- Target board: `VREsp32-v1` (`boards/VREsp32-v1/hwdef.vrp`).
